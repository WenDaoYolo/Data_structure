

指针
```
首先若想要操作一个内存单元，就必须得通过该内存单元的地址来定位
而指针就是用来存放内存单元地址的变量，让我们能通过指针操作内存

先前提到我们无法直接操作物理层，所有操作都是通过逻辑层间接进行
操作逻辑指针中存放的逻辑地址，底层转换后由物理指针操作物理地址
```

指针操作符
```
x *y：  定义x类型的指针y，用来存放x类型的内存地址
&x：    &取地址运算符又称引用，表示取x所在内存单元的地址
*x：    *取值运算符又称解引用，表示取地址x对应内存中存储值
```
---

静态内存申请与释放
```
在栈区上进行的操作例如函数调用、局部变量定义等，编译器会自动帮我们管
理和维护，使用时帮我们申请内存，调用结束、变量销毁等会帮我们回收内存
```

动态内存申请与释放
```
在堆区上进行的操作例如动态内存分配与释放等，需要手动管理内存
c中使用malloc/calloc/realloc与free来申请和释放内存
c++中使用new与delete/delete[]来申请和释放内存
```
---
该章节内容语法与特性详见c/c++指针、动态静态内存分配