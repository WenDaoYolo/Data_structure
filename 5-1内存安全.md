

该章节讲述使用工具操作内存和其他的过程中会出现一些错误与风险，以及对应的防范措施和安全机制
```
1.指针使用
2.动态内存      
3.缓冲区溢出
4.Stack Canary、ASLR、PIE、RTC、代码执行权机制(NX,XD,XN,DEP)
```
---
指针使用时常见的错误(c/c++同理)


1.指针可以存放自己的地址，但这种行为是未定义，编译器会报错
```
会出现类型错误(指向自己其指向的类型是无法确定的，指针的类型是一级指针还是二级，存在二义性)
```

2.未检查地址边界，指针非法越界访问，指向的空间是非法的，如某些重要的数据被修改或丢失则会有巨大风险
```
int arr[10]={0},int* p=arr;
for(int i=0;i<=10;i++)
{
	printf("%d\n",*p)
	p++;			       //数组最大元素为arr[9],而在i=10时，p的指向超出了数组的范围即越界
}					       //越界非法访问内存，访问了一个未知地址
```

3.野指针(wild pointer)，指针未初始化时会指向随机地址，指针指向不确定时，会出现内存非法越界访问的情况
```
int* p;				      //指针未初始化,则会指向随机地址
*p=20				      //操作了不属于当前程序的内存单元，属于非法访问内存，只有申请了内存空间，才能使用该内存空间
```

4.悬空指针(Dangling pointer)，指针指向的内存区域已经释放或者无效，同理也会出现内存非法越界访问的情况
```
int* test(){               //int*类型，即整型指针(地址)
	int a=20;
	return &a;             //返回的局部变量a的地址
}

int main(){
	int* pa=test();        //函数调用结束后，局部变量a则会销毁，此时pa指向了一个未知的地址
	*pa=30;                //非法访问内存单元，编译可能不会报错，但会有一些潜在风险，要避免
	printf("%d\n",*pa);   
}
```

5.指针使用前不判断有效性，对空指针进行取值操作
```
int* a=NULL;
*a=200;
```
---
指针使用时常见的防范方法


1.及时初始化
```
定义即初始化：一个良好的编程习惯,不管是指针还是其他都能有效减少错误,若暂时没有确定值，可以初始化为空
```

2.检查指针是否越界
```
c语言本身不会检查数据越界行为，在编写代码时要额外注意
```

3.内存空间释放后，及时将相关的指针置空
```
c中使用NULL，c++中使用nullptr
```

4.在使用指针前检查其有效性
```
尽可能保证指针只存在两种状态：有效地址和NULL，指针用的时候指向有效地址，不使用或者失效时置空	
```

5.防御性编程，例如使用assert断言函数(头文件assert.h)
```
void test()
{
	int* a=NULL
	assert(a!=NULL);         //断言a不为空指针，断言成功程序继续执行，否则程序终止
}
```

6.使用RAII机制，Resource Acquisition Is Initialization资源获取即初始化
```
将资源的申请和释放与对象的生命周期进行绑定，即对象生成后立马申请资源(c++构造函数)，对象销毁前释放资源(c++析构函数)
例如c++的智能指针类：unique_ptr、shared_ptr、weak_ptr
```
---
动态内存分配的常见错误(c/c++同理)


内存泄漏，动态分配的内存未释放，无法通过地址再次找到该内存空间，该内存就无法再使用
```
对于一些需长时间运行的程序如服务器，是非常严重的问题，内存频繁的泄漏最后会造成内存耗尽的情况
内存耗尽后服务器就会卡死，最后只能通过重启机器强制结束程序来释放内存，这样可能会造成数据丢失
若不从根源上解决内存泄漏，服务器每隔一段时间得频繁地重启，这样不仅会降低效率还会折损硬件寿命
所以动态开辟的内存要用指针记录，内存不使用后要及时释放，为了避免非法访问相应的指针也应该置空
```

1.不判断，直接对指针进行解引用操作
```
int main(){
	int* ptr=(int*)malloc(1000000000000);   //开辟的空间过大，所以可能会开辟失败，返回空地址
	int i=0;
	for(i=0;i<10;i++){
		*(ptr+i)=i;
	}
	return 0;
}
```

2.对动态开辟的内存空间进行越界访问
```
int main(){
	int* ptr=(int*)malloc(10*sizeof(int));
	if(ptr==NULL)
	{
		perror("main");
		return;
	}
	int i=0;
	for(i=0;i<20;i++){
		*(ptr+i)=i;
	}
	return 0;
}
```

3.使用free释放非动态开辟的内存空间
```
int main(){
	int arr[10]={0};
	int* ptr=arr;
	free(ptr);
	ptr=NULL;
	return 0;
}
```

4.使用free只释放动态开辟内存的一部分(会造成内存泄露的问题)
```
int main(){
	int* ptr=(int*)malloc(10*sizeof(int));
	if(ptr==NULL)
	{
		perror("main");
		return;
	}
	int i=0;
	for(i=0;i<5;i++){
		ptr++;
	}
	free(ptr);            //只释放了40个字节内存空间的后20的字节
	ptr=NULL;             //然后将指针置为空后，前20个字节的内存空间就找不到了，所以只部分释放是错误的，很容易造成内存泄露的问题
	return 0;
}
```

5.对已经释放的内存空间再次进行访问(非法内存的访问，所以内存释放后相应的指针应该及时置空)
```
int main(){
	int* ptr=(int*)malloc(sizeof(int));
	free(ptr);
	*ptr=30;              //指针指向的内存已经被回收，此时这块内存不属于当前程序使用的范畴，属于非法内存访问
	return 0;
}
```

6.对同一块动态内存空间进行多次释放(同理，也属于非法内存访问)
```
int main(){
	int* ptr=(int*)malloc(40);
	...
	free(ptr);
	...
	...
	free(ptr);
	return 0;
}

//当程序代码过多时很容易出现重复释放的问题，最好的解决方法就是释放完后对指针立马置空
//当再次对该指针指向的内存空间进行释放时，此时为NULL，free传参NULL则什么也不会发生
```

7.动态开辟的内存空间使用完后未释放(会造成内存泄露的问题)
```
void test(){
	int *ptr=malloc(10*sizeof(int));    //ptr指针出了函数则会自动销毁，若不对开辟的空间进行销毁，这块空间就会泄漏
}

int main(){
	while(1){
	...
	test();
	...
	}
	return 0;
}
```
---
缓冲区溢出攻击实验


攻击原理
```
例如堆栈溢出(StackOverFlow)，这里的堆栈就是栈，即先前的遗留问题，与堆无关
栈区中内存向下生长，复合变量中内存向上生长，局部变量存储在低地址处，返回地址存储在高地址处
从局部变量开始输入数据，若数据的长度大于存储空间定义的大小，溢出部分则会向上覆盖其他的空间

ebp存储区被覆盖程序不会崩溃，但会导致栈帧混乱，但会存在隐藏风险
返回地址被覆盖，若地址无效则会导致程序找不到执行点而崩溃(段错误)
若是恶意代码的地址，则函数结束后会弹出并跳转至恶意代码处执行代码
```

实验前言
```
注：现代编译器可能会采用相关保护机制，如有则需提前关闭
实验环境：windows11 x86-64、tdmgcc(无常规保护机制)
假设恶意代码地址：0x1122334455667788

void bad_code()
{
	//启动本地终端、提升操作权限...
	//入侵成功，开始执行恶意操作...
}
```

代码
```
void test1()
{
    char arr[4];   //为了方便实验，此处长度设计为4不需要考虑内存对齐
    cin>>arr;
    cout<<arr;
}

int main()
{
    test1();
    return 0;
}
```

攻击者视角
```
1.输入123456789后自动补'\0'
1234写入局部变量区，56789\0覆盖ebp存储区
溢出的无效地址未覆盖返回地址，程序未崩溃

2.输入123456789123后自动补'\0'
1234写入局部变量区，56789123覆盖ebp存储区
\0部分覆盖返回地址，返回地址损坏程序崩溃

3.输入123412345678\x88\x77\x66\x55\x44\x33\x22\x11后自动补'\0'
1234写入局部变量区，12345678覆盖ebp存储区
\x88\x77\x66\x55\x44\x33\x22\x11(小端存储)覆盖返回地址，\0向上覆盖
函数返回时，弹出并跳转至0x1122334455667788恶意代码处
```

总结
```
攻击者即使无法编写程序，也能运行程序精确构造输入从而来控制程序
而现代系统采用Stack Canary、ASLR+PIE、NX等机制防御此类攻击
```
---
现代内存的保护机制
```
1.stack canary
2.RTC(rtcs、rtcu、rtcc)
3.代码执行权机制
4.ASLR+PIE(详见现代内存的机制)            
5.编译器可能存在的优化(详见栈区深度解析)                          
6.人为保护，如缓冲区长度检查等(也是gets等函数被淘汰的原因)

不同环境采用不同组合机制，具体结合编译器、系统版本等在需要时自行查阅
```

stack canary机制
```
在返回地址下插入一个哨兵，当哨兵被修改时则判断为栈溢出
此时会立即中断程序并报错，报错信息：已终止(核心已转储)
不仅能中断程序防止继续入侵，还能给出信息方便程序的调试

canary命名的由来
译为金丝雀，对有有毒气体敏感，会直接导致死亡
所以早期矿洞等危险工作都用金丝雀作为信号预警

Linux中stack canary的关闭方法
编译选项：gcc/g++ -fno-stack-protector ...
```

stack canary开启(linux_gcc)
![[stack_canary.png]]

stack canary关闭(linux_gcc)
![[stack_canary_shutdown.png]]

代码执行权机制
```
源程序的函数指令在编译期间就写入了代码段，运行期间不可改变，启用NX后cpu
只会执行代码区的指令，攻击者即使将恶意代码的地址写入，而地址指向的代码指
令不在代码段，cpu则不会跳转执行，这样能很好地辨别出源程序代码和外来代码

对于不同系统架构，其代码执行权机制的命名也不同
amd、linux：NX(No eXeCutable，NE术语已存在)
其他：XD、XN、DEP...
```

现代系统中通常采用的多层防御体系
```
第一道防线[ASLR + PIE]         让攻击者找不到地址
第二道防线[stack canary]       即使攻击者找到地址，让攻击者也无法继续
第三道防线[代码执行权机制]       即使攻击者绕过哨兵，让攻击者也无法执行
```

RTC机制
```
RTC：RunTime Check，msvc debug模式下默认采用，分为rtcs、rtcu、rtcc

rtcc：RunTime Check Conversion，检测数据丢失的转换
rtcu：RunTime Check Uninitialized，检测未初始化的变量
rtcs：RunTime Check Stack_frame，检测栈帧损坏(变量越界)
rtcs保护整个栈区，在每个变量之间都插入哨兵，只要越界就会中断报错
```

rtcs(vs2022-msvc)
![[rtcs.png]]