
物理与逻辑的映射
```
物理即真实存在的硬件，逻辑是思维上抽象出来的，他们之间建立了对应的关联就称之为映射
同时映射不只是一种技术的实现，更是一种思维方式，在许多方面都有涉及例如算法、逻辑等
```

为什么需要映射
```
对于硬件，操作起来非常的麻烦复杂低效，且容易造成硬件的损坏，所以我们不能直接去操作硬件
于是就出现了映射的思维，映射建立逻辑上的关联，然后以软件的方式间接管理和操作关联的硬件

所以完整的计算机系统由硬件+软件组成，硬件提供设备基础(身体)，软件提供管理和使用的接口(灵魂)
```

操作系统OS(option system)
```
操作系统是整个计算机从物理到逻辑的映射，所有硬件资源都被映射为逻辑的形式，然后通过操作系统管理
是一个逻辑资源映射的集合，是特殊的软件，我们可以通过这个环境高效方便地去管理各种软件和硬件资源
针对不同机器的环境和配置，同样有不同版本的操作系统，例如32位、64位windows或linux操作系统等
操作系统向下兼容不向上兼容，64位os同时装载32位和64位机器环境，32位操作系统只装载32位机器环境

所以在装配电脑时，不仅硬件需要匹配，逻辑端进行管理的操作系统也要匹配
如32位操作系统最多能识别和管理4GB左右的内存，超过4GB的部分则无法识别
所以现在装配操作系统时当内存空间大于4GB时，同理应该装配64位的操作系统
```

操作系统的运行和工作原理
```
1.计算机装载对应的操作系统
2.开机启动后自动运行操作系统
3.通过OS间接操作和管理软硬件,所有操作都基于OS
4.操作系统的用户态与核心态机制(详见专业通识的文件篇)
```

地址、指针的逻辑与物理映射
```
OS中的内存地址就是物理地址的映射，是逻辑上的地址
用户不能直接操作物理硬件，需通过逻辑地址间接操作物理地址

同理，OS中的指针就是物理指针的映射，是逻辑上的指针
用户不能直接操作物理硬件，需通过逻辑指针间接操作物理指针
```
---

基址(连续内存的首地址)
```
不同类型的数据占用不同的空间，对于占用空间大于一字节的数据，会为其分配多个连
续的内存单元，这些连续内存单元的首地址会作为数据的地址，而这个地址又称为基址
```

```
int a = 4;
int 类型占用4个字节,内存会分配4个连续地址的内存单元 假设地址分配为0x000000D6C0CFFB64(机器字长为64位即8个字节)

实际占用的4个内存单元地址分别为：
0x000000D6C0CFFB64	                 多个连续的内存单元，使用首地址作为变量的地址(&a=0x000000D6C0CFFB64)              
0x000000D6C0CFFB65                   一个地址管理一个内存单元(大小为1字节)，int占用4个内存单元即4字节空间
0x000000D6C0CFFB66
0x000000D6C0CFFB67
```
---
  
进程的基本概念
```
程序=指令+数据，cpu执行指令去处理数据并得到结果

程序是静态的指令数据，是存储在硬盘等外存的文件
进程是正在运行的程序，程序从外存加载到内存运行
```

虚拟内存技术
```
为什么需要虚拟内存技术？

1.安全，进程之间进行了隔离
早期程序运行是直接使用内存的，多个进程加载到内存中，他们都以为自己独占整个内存
进程之间没有了内存边界的概念，导致他们之间可能会互相影响和修改从而系统混乱崩溃

2.灵活，程序可以在不同物理位置运行
不需要为其分配固定的物理内存地址，每次运行时只需要为其分配一个空闲的逻辑地址即可

3.简化，程序员不用关心物理内存布局
底层的物理内存布局由操作系统和硬件自动进行管理和维护，我们只需要关心逻辑地址即可

4.共享，虚拟内存技术可以实现内存共享
多个进程的虚拟内存空间可以映射到同一物理内存空间，从而实现内存的共享，例如动态库


所以就出现了虚拟内存技术，其本质就是基于映射思维的一种技术手段
逻辑上对每个进程划分内存，进程之间的内存空间相互独立，互不干扰
逻辑内存地址到物理内存地址的映射由OS、MMU和页表机制来进行转换

未采用虚拟内存技术:
++++++++++++++++
+  进程1        +     ->内存块1
+  进程2        +     ->内存块1
+  进程3        +     ->内存块1
++++++++++++++++

采用虚拟内存技术后:
++++++++++++++++
+   进程1       +     ->内存块1-1，假设虚拟地址为0x0000映射到物理地址为0x1211
++++++++++++++++   
+   进程2       +     ->内存块1-2，假设虚拟地址为0x0000映射到物理地址为0x35a6
++++++++++++++++
+   进程3       +     ->内存块1-3，假设虚拟地址为0x0000映射到物理地址为0x77ff
++++++++++++++++
```

分页机制
```
MMU：memory management unit，内存管理单元，是专门用于内存管理的硬件
分页：物理内存和虚拟内存都会划分为固定大小的页(通常4kb)，每一页都有编号
页表：用于记录虚拟页号到物理页号的映射关系，由操作系统自动更新管理和维护

缺页中断：当访问的逻辑页在物理页中没有映射时，会触发缺页中断机制
1. CPU请求缺页中断
2. OS从磁盘读入该页
3. 更新页表
4. 重新执行指令
```