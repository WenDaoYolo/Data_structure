
c/c++线程库
```
C语言并没有自己的标准线程库(需要使用其他平台提供的库)，而C++有自己的标准线程库(头文件thread)
平台提供的线程库，由于不够通用，所以本次线程篇学习以C++标准库为主，后续如果需要再学习其他平台库

早期C语言没有自己的标准线程库，而又迫于C程序开发需要多线程，于是其他平台就开发提供了自己的线程库
linux/unix提供了pthread.h，windows提供了CreateThread API，久而久之便成了各编译器支持的标准
后期C语言即使制定了标准的线程库，但由于使用不广泛很多编译器不支持，最终导致C语言没有标准的线程库
```

线程的创建
```
线程以函数的方式创建运行，进程默认情况下会创建一个主线程运行main函数
一个线程中可以创建另一个线程，创建者称为主调线程，被创建者称为被调线程
线程被创建出来后只会进入任务池，其执行顺序和时长不可控，均取决于调度器
```

c++中线程实际上是一个thread类对象，通过构造函数传入回调函数(要运行的函数)以及参数
```
void fun1(int a,int b){ std::cout<<"thread1"<<std::endl; }
void fun2(){ std::cout<<"thread2"<<std::endl; }
void fun3(){ std::cout<<"thread3"<<std::endl; }

int main()
{
	std::thread t1(fun1,666,999);
	std::thread t1(fun2);
	std::thread t1(fun3);
	return 0;
}
```
![[MultiplexThread.png]]

同步与异步行为
```
实体之间的执行关系，同步表示某一实体等待其他实体执行完后再执行，异步表示无需等待直接执行
```

线程的生命周期
```
被调线程执行完成后资源会被回收，但如果主调线程执行完毕，被调线程即使还没有执行完也会被强制终止，然后资源会被回收
而主线程运行的main函数是整个程序的生命周期，所以无论是局部的主调或被调线程还是全局线程，进程结束后所有资源被回收
```

线程强制终止的风险
```
由于线程共享进程资源的特性，线程之间可能存在依赖关系，若持有重
要资源的线程被强制终止，可能导致进程崩溃，这也是线程通信的缺点

例如：
进行业务逻辑的数据读写时，重要数据在读写到一半时被强制终止，从而导致重要数据丢失
线程间进行通信和限制的公共资源丢失，例如线程锁的丢失则会导致线程产生死锁(见后续)

所以我们需要对线程的生命周期进行管理，从而保证线程不会被强制终止，但由于线程的执行顺
序与时长不可控，无法确定其释放时机，所以需要通过同步和异步的行为来控制线程的生命周期
```

线程生命周期的管理
```
c++中通过类方法join和detach来实现同步和异步行为，从而控制线程的生命周期
join是同步阻塞方法，主调线程调用被调线程的join方法后，会等待被调线程执行完毕后再执行，保证被调线程不被强制终止
detach是异步非阻塞方法，主调线程调用被调线程的detach方法后，会将被调线程分离并转入后台运行，其生命周期由OS接管

链式同步：
要避免过深的链式同步，会导致某一线程长时间阻塞，并且中间有任何一个线程阻塞
都会导致整条线程链阻塞，例如主线程等待t1执行完毕，t1等待t2，t2等待t3....
```

terminate函数机制
```
程序中的异常错误，根据风险等级分为可处理的异常和严重风险的错误，可处理的异常通过异常机制处理
而严重风险的错误通过terminate处理，其默认行为是直接终止进程，并报错输出terminate without
an active expection(意为没有活动异常的终止)，这样不仅能用可控的崩溃来防止不可控的错误，还
能区分程序的错误类型，从而有利于程序调试，也可通过set_terminate自定义terminate的处理行为

一个线程创建出另一个线程后，必须通过join或detach对其进行生命周期的管理，而对于全局的线程，只要
保证能在主线程结束之前不被强制终止即可，所以任何线程都可以对其进行管理，若不对其进行管理，线程在
销毁前会调用析构进行检测，若没有指定join或detach行为，会作为严重风险错误，通过terminate来处理
```
