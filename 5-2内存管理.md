

内存对齐
```
内存对齐用于连续内存中不同类型数据的排列，例如结构体、类、数组内存，而最终目标是对齐整块内存区
结构体类等复合局部变量内部对齐后，最后整体外部也需要对齐(所以需要额外填充为最大对齐数的整数倍)

同理函数栈帧内部对齐后，外部最终还需要对齐到整个栈区，还有堆区内存对齐等，但这些都过
于底层暂不需要深入研究，只需掌握函数栈帧内的内存对齐和复合局部变量成员的内存对齐即可
```

内存对齐的作用：使不同类型的数据在内存中存放的更加整齐，提高数据访问的速度和效率，是一种空间换时间的方法
```
1.硬件或平台编译器的原因：某些设备或编译器平台不支持在任意地址上访问任意类型的数据
例如某平台其访问指针的偏移是固定的，只允许在4的整数倍地址处访问数据，否则会抛出硬件异常
所以内存对齐使数据存放更加整齐，任何类型的数据都存放在4的整数倍地址处，使硬件能访问数据

2.性能原因：因为硬件原因，访问数据的指针其偏移步长是固定的，所以对于未对齐的内存
可能需要分多次才能读取一个的完整数据，而内存对齐后，只需要一次就能读取到完整数据
```
---
 
内存对齐规则(例如结构体，若成员类型一样则无需对齐，否则需要对齐)
```
1.首成员无需对齐，直接从结构体基址处开始存放

2.结构体其他成员依次从对齐数整数倍的地址开始存放
对齐数即成员大小和默认对齐数中的较小值，单位字节，不同编译器默认对齐数不同
MSVC默认对齐数8，gcc无默认对齐数(即以成员自身大小为对齐数)，但可人为设置

3.结构体最终大小还需要对齐到最大对齐数的整数倍，以便栈帧中的内存对齐

4.成员是复合类型如数组结构体类，则要先对齐，再取最大对齐数作为该成员对齐数
```

结构体内存布局分析1：环境vs2022+msvc(对齐数的差异)
```
#pragma pack(2)       //默认对齐数设为2

struct v1
{
	short d;          //偏移0~1
	int c;            //偏移2~5
	char e;           //偏移6
};                    //最终对齐大小8个字节，最大对齐数2

struct v2
{
	char a;           //偏移0
	char b;           //偏移1
	struct v1 vv;     //偏移2~9
};                    //最大对齐数2，最终对齐大小10个字节
```

结构体内存布局分析2：环境ubuntu-linux+gcc(对齐数的差异)
```
struct v1             //以成员自身大小为对齐数
{
	short d;          //偏移0~1
	int c;            //偏移4~7
	char e;           //偏移8
};                    //最终对齐大小12个字节，最大对齐数4

struct v2
{
	char a;           //偏移0
	char b;           //偏移1
	struct v1 vv;     //偏移4~15
};                    //最大对齐数4，最终对齐大小16个字节
```
---
栈帧内存布局分析
```
详见内存模型栈区深度解析，注：局部变量区内存布局完全取决编译器，可简单实验但无法精准预测(除非想研究开发编译器)
```

例题分析
![[程序分析.png]]
```
假设局部变量区向下生长，则i位于高地址处，数组位于低地址处，所以数组向上越界有可能访问到i并将其修改为0，于是程序就陷入了死循环
但对于现代编译器来说有很多不确定性因素，首先是具体内存布局编译器会进行优化，其次就是可能会采用相关的保护机制例如stack canary
```
---
内存的布局与设计：不要无节制的去用空间换取时间，寻找一个平衡点既能保证时间又能保证空间


1.成员的顺序会影响内存的对齐(尽可能使空间相近的成员定义在一起)
```
struct test1{      
	char a;       //偏移0
	float b;      //偏移4~7
	char c;       //偏移8
};                //最终对齐大小：12个字节

struct test2{     
	char a;       //偏移0
	char c;       //偏移1
	float b;      //偏移4~7
};                //最终对齐大小：8个字节

gcc取自身为对齐数，msvc取成员与8中的较小值，所以该案例gcc和msvc下结果都一样
```

2.修改默认对齐数，使用pragma pack预处理命令
```
对齐数一般设为2的次方不会设为奇数，默认对齐数为1时等价于不对齐，无参则还原为系统默认对齐数

#pragma pack(参数)            //修改默认对齐数
...
#pragma pack()               //还原默认对齐数
```
---
内存池基本思想(Memory Pool)










