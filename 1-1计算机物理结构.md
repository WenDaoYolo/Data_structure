
计算机五大体系结构：控制器、运算器、存储器、输入设备、输出设备
```
cpu主要由控制器、运算器构成，存储器包括寄存器、缓存、内存(主存)、外存(辅存)等

计算机内部:包括cpu、内存
计算机外部:包括外存、输入输出设备
```

各类存储器的存储速度：高速缓存&寄存器>内存>外存
```
外存：容量大，速度低，成本低                    可以永久存放数据，断电后数据不会丢失
内存：容量小，速度快，成本高                    用于暂存数据，断电后数据会丢
高速缓存、寄存器：容量更小，速度更快，成本更高     用于暂存数据，断电后数据会丢失

根据各类存储器的特性以及性价比，他们在计算中都发挥着各自重要的作用
```

计算机处理数据的大致过程：cpu负责数据的处理和运算，存储器负责数据的存储，具体细节后续研究
```
1.
外存中的数据临时加载到内存之中，然后cpu再从内存中取数据进行处理和运算

2.
cpu与内存会出现速度不匹配的问题，内存存取数据的速度跟不上cpu运算的速度，导致cpu会长时间
地进行等待，从而大大降低了工作效率，于是在内存与cpu之间增加了寄存器和高速缓存来进行调节

3.
高速缓存(cache)又分为一级、二级、三级三个级别，用于调节cpu与内存之间的速度，内存数据送往缓存，然后cpu从缓存中取数据
寄存器regisiter,用于进一步提升数据的存取速度，对于频繁重复使用的数据会存放到寄存器，需要使用时cpu会直接从寄存器存取

4.
cpu处理完数据后，将其结果送往内存进行临时存储，再由内存送往外存来进行永久存储，所以程序和数据都要加载到内存中才能运行和处理
```

多级缓存
```
一级缓存：最快，通常几十kb，cpu核心独占
二级缓存：中等，通常几百kb，可能共享
三级缓存：最慢，通常几mb，所有核心共享

缓存的命中率：cpu从缓存中读取到所需数据的几率，是程序优化的重要指标
cpu取数据时首先会访问寄存器和高速缓存，若没有需要的数据再从内存中取数据
```

cpu访问一次存储的所需要的时间参考
```
cpu寄存器         <1ns
一级缓存          2-4ns
二级、三级缓存     10-20ns
内存             40-90ns
外存             0.1-10ms
```

计算机的特性
```
由于计算机的底层特性只能识别两种状态(电平信号)，这刚好对应二进制，所以计算机内部所有的数据都是以二进制的形式处理和运算
```

总线BUS：计算机中数据传输的线路
```
分为数据总线DB(Data Bus)，地址总线AB(Address Bus)，控制总线CB(Control Bus)

数据总线：用于传输数据，其长度决定一次性传输数据的大小，如64位可以传输8个字节的数据
地址总线：用于机器寻址，其长度决定机器的寻址范围(即机器可以访问和使用多大的内存空间)
控制总线：用于传输控制信号，与长度位数无关
```

机器的位数
```
我们常说的64位机器通常指的是一种综合概念(32位同理)

1.数据总线的长度为64位，cpu一次性可以传输和处理64位即8个字节的数据，所以又称为cpu的位数
2.地址总线的长度为64位，即机器寻址范围最高可达16EB(实际受主板等硬件限制，且无需过大内存)
3.cpu使用64位的机器指令集(例如x86-64,AMD-64等)
```

32/64位地址总线的寻址能力
```
一根物理线有通电和不通电两种状态，用二进制位来表示状态只需一位(0和1)

同理，32根地址线需要32位，可以表示2^32种状态(00000000...00000000 - 11111111...11111111)，每种状态都可表示一个内存
单元的地址，通过地址来访问和管理内存单元，所以32位机器可以管理2^32 个内存单元(即4GB内存)，其地址范围也称位机器的寻址范围
同理，64位机器理论最大可管理2^64个内存单元(即16EB)的内存空间...

所以在装配电脑硬件时，内存的大小要和机器的位数相匹配，例如32位机器若装的内存大于4GB时，则超出寻址范围的部分无法识别会浪费
```

内存Memory
```
内存被划分为若干内存单元使用，每个内存单元的大小为一个字节，并拥有自己的
地址(动态分配)，cpu通过地址找到对应的存储单元，然后访问内存单元中的数据

计算机中，最小数据单位是位(bit)，基本存储单位是字节(Byte)
```

 内存划分以及地址分配的意义：提高访问的速度，并且方便管理和使用
```
例如酒店划分为多个房间，给每个房间编号后有序地进行管理，然后通过地址号码可以快速定位到房间的位置
```

计算机内部虽然采用二进制管理地址，但为了方便用户管理，会以更精简的16进制展示(内存中采用0x标识)
```
空间编址从0开始没有负数,所以地址是正整型常量

00000000 00000000 00000000 00000000B->0x00000000
00000000 00000000 00000000 00000001B->0x00000001
00000000 00000000 00000000 00000010B->0x00000002
...
```

计算机中单位的换算：除字节与位，其他的单位之间的换算大小都是1024
```
1 Byte = 8bit
1 KB = 1024Byte 
1 MB = 1024KB
1 GB = 1024MB
1TB = 1024GB
1EB = 1024TB
```

进制的后缀标识和转换
```
二进制B：满二进一，基数只有0和1
八进制O：满八进一，基数0-7
十进制D：默认不使用后缀，基数0-9
十六进制H：满十六进一，基数0-9，A-F(10-15)

8转2：421法
10转2：短除法、按权定位法
16转2：8421法
2转任何进制：按权展开法
```





